# Only the packages needed for: FastAPI/MCP server + sport control + state reading.
#
# Installed separately (not via pip):
#   - CycloneDDS (C library)   -> build from source, export CYCLONEDDS_HOME
#   - unitree_sdk2_python      -> git clone + pip install -e .
#   - rclpy                    -> NO LONGER NEEDED. State is read straight over DDS via the SDK.

# --- Web server + MCP ---
fastapi==0.116.1
fastapi-cli==0.0.8
fastmcp==2.11.3
httptools==0.6.4
pydantic==2.12.5
pydantic-settings==2.12.0
dependency-injector==4.48.1
aiofiles==24.1.0

# --- Dependencies of unitree_sdk2py ---
numpy==2.4.0
opencv-python==4.10.0.84

# --- Test ---
pytest
pytest-asyncio
